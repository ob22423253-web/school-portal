[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# SPA history-mode redirect so refreshes on /dashboard etc don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
